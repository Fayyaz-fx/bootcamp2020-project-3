import React from 'react'
import { useParams } from 'react-router'
import { getBrandList } from '../data/Data'

const Brand = () => {
  // console.log(useParams())
  // let list = getBrandList(name)
  // console.log(list)
  return (
    <div>
      <h1>Hello</h1>
    </div>
  )
}

export default Brand
